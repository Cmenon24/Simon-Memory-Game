package simon.model;

/**
 * SimonColor represents the 4 possible buttons/colors in the Simon game.
 * Using an enum prevents mistakes like misspelled strings ("gren", "blu", etc.).
 */
public enum SimonColor {
    RED, GREEN, BLUE, YELLOW
}
